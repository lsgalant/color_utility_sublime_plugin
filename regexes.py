rgb_hex = ''.join([
	'(#)',
	'([0-9A-Fa-f]{2})',
	'([0-9A-Fa-f]{2})',
	'([0-9A-Fa-f]{2})'
])

rgba_hex = '(#)([0-9A-Fa-f]{8})'

rgb = 'rgb()'
rgba = 'rgba()'

hsl = ''.join([
	'(hsl)',
	'(\()',
	'((360|3[0-5][0-9]|[0-2][0-9][0-9]|\d{1,2})(\.\d+)?)',
	'(, ?)',
	'((100|\d{1,2})(\.\d+)?)',
	'(%, ?)',
	'((100|\d{1,2})(\.\d+)?)',
	'(%\))'
])

notag_rgb_a = ''.join([
	'(\d.\d\d)',
	'( )',
	'(\d.\d\d)',
	'( )',
	'(\d.\d\d)'
])

# hsl_notag = ''

# hsla = ''

# hwb = ''

# notag_a = ''.join([
# 	'(\()',
# 	'([0-2][0-5]{2}|\d{1,2})',
# 	'(\.\d+)?',
# 	'(, ?)',
# 	'([0-1]00|\d{1,2})',
# 	'(\.\d+)?',
# 	'(\.\d+)?',
# 	'(, ?)',
# 	'([0-1]00|\d{1,2})',
# 	'(\.\d+)?',
# 	'(\.\d+)?',
# 	'\)'
# ])